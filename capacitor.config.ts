import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RegistroApp30',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
